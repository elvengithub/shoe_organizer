# Raspberry Pi 4: use python3 -m venv venv && source venv/bin/activate
flask>=3.0.0
# AI camera uses OpenCV heuristics in src/ai_camera.py; optional later: tflite-runtime + .tflite model on Pi
opencv-python-headless>=4.8.0
# Optional ONNX shoe detector (jmaloney95/Shoe-ObjectDetection-and-ImageClassification–style); gate only
onnxruntime>=1.16.0
onnx>=1.14.0
numpy>=1.24.0
Pillow>=10.0.0
PyYAML>=6.0
python-dotenv>=1.0.0
pyserial>=3.5  # ESP32 USB serial telemetry (serial_bridge)

# Two-stage YOLOv8 + PyTorch classifiers (optional; heavier — PC / Jetson):
# pip install -r requirements-ai.txt

# Pi runtime for Phase 2 TFLite shoe model (optional):
# tflite-runtime>=2.14.0

# Install on Pi only (GPIO + optional sensors):
# RPi.GPIO>=0.7.1
# adafruit-circuitpython-dht  # or use I2C BME280 modules on a bus multiplexer
